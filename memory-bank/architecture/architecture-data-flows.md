# Pinterest Pipeline Architecture & Data Flow Reference

**Compiled:** 2026-02-25
**Purpose:** Definitive reference for all data flows, naming conventions, file paths, column layouts, and cross-file dependencies. Consult this before making ANY pipeline changes.

---

## Table of Contents

1. [Pipeline Overview](#1-pipeline-overview)
2. [ID & Naming Conventions](#2-id--naming-conventions)
3. [File Path Conventions](#3-file-path-conventions)
4. [Image Flow (Detailed)](#4-image-flow-detailed)
5. [Pin Assembly & Template Variables](#5-pin-assembly--template-variables)
6. [Data File Schemas](#6-data-file-schemas)
7. [Google Sheets Column Layout](#7-google-sheets-column-layout)
8. [GCS Upload Flow](#8-gcs-upload-flow)
9. [Blog Deployment & Pin Schedule](#9-blog-deployment--pin-schedule)
10. [Daily Posting Flow](#10-daily-posting-flow)
11. [Content Log & Analytics](#11-content-log--analytics)
12. [Apps Script Triggers](#12-apps-script-triggers)
13. [LLM Model Calls](#13-llm-model-calls)
14. [Workflow Execution Order](#14-workflow-execution-order)
15. [Cross-File Dependency Map](#15-cross-file-dependency-map)
16. [Environment Variables](#16-environment-variables)

---

## 1. Pipeline Overview

```
Plan → Generate → Review → Deploy → Post → Analyze → (feeds next Plan)
```

| Step | Trigger | Script(s) | Output |
|------|---------|-----------|--------|
| 1. Weekly Plan | Monday 6am ET cron | `generate_weekly_plan.py` | `data/weekly-plan-W{N}-{date}.json`, Weekly Review sheet |
| 2. Plan Approval | Sheet B3 = "approved" | Apps Script → `generate-content.yml` | — |
| 3a. Blog Generation | Workflow | `generate_blog_posts.py` → `blog_generator.py` | `data/generated/blog/{slug}.mdx`, `data/blog-generation-results.json` |
| 3b. Pin Generation | Workflow | `generate_pin_content.py` | `data/generated/pins/`, `data/pin-generation-results.json` |
| 3c. Publish Queue | Workflow | `publish_content_queue.py` | GCS URLs, Content Queue sheet |
| 4. Content Review | Human in Sheet | — | Status per row in Column J |
| 4b. Regen (optional) | Sheet N1 = "run" | `regen_content.py` | Updated pins/images/sheet rows |
| 5. Deploy Preview | All rows terminal | `blog_deployer.py` → `deploy_to_preview()` | Blogs on goslated.com develop branch |
| 6. Production Approval | Sheet B4 = "approved" | Apps Script → `promote-and-schedule.yml` | — |
| 7. Promote & Schedule | Workflow | `blog_deployer.py` → `promote_to_production()` | `data/pin-schedule.json`, `data/content-log.jsonl` |
| 8. Daily Posting | 3x daily cron | `post_pins.py` | Live Pinterest pins, updated `content-log.jsonl` |
| 9. Analytics | Monday 6am ET | `pull_analytics.py` → `weekly_analysis.py` | Updated metrics in `content-log.jsonl` |
| 10. Monthly Review | 1st Monday | `monthly_review.py` | Strategy recommendations |

---

## 2. ID & Naming Conventions

### Post IDs
- **Format:** `W{week_number}-P{seq}` (e.g., `W12-P01`, `W12-P08`)
- **Generated by:** Claude in `generate_weekly_plan()` response
- **Unique within:** A single week
- **Used in:** weekly plan JSON → blog-generation-results.json → Content Queue sheet → content-log.jsonl

### Pin IDs
- **Format:** `W{week_number}-{seq}` (e.g., `W12-01`, `W12-28`)
- **Generated by:** Claude in `generate_weekly_plan()` response
- **Unique within:** A single week
- **Used in:** weekly plan JSON → pin-generation-results.json → pin-schedule.json → Content Queue sheet → content-log.jsonl

### Pin-to-Blog Linkage
- **Field:** `source_post_id` on pin specs → parent blog's `post_id`
- **Example:** Pin `W12-01` has `source_post_id: "W12-P01"`
- **Used for:** Deriving blog URL for pin link, mapping hero images between blog/pin, grouping pins under their parent blog

### Blog Slugs
- **Format:** lowercase-hyphenated (e.g., `easy-weeknight-chicken-tacos`)
- **Generated by:** Claude in blog post frontmatter `slug:` field
- **CRITICAL:** goslated.com routes by FILENAME, not frontmatter slug. The filename IS the slug.
- **Used for:**
  - Blog file: `data/generated/blog/{slug}.mdx`
  - Blog URL: `https://goslated.com/blog/{slug}`
  - Blog hero image: `data/generated/pins/{slug}-hero.{ext}`
  - Deployed blog: `content/blog/{slug}.mdx` on goslated.com repo
  - Deployed hero: `public/assets/blog/{slug}{ext}` on goslated.com repo

### Image Filenames
| Type | Pattern | Example | Where Saved |
|------|---------|---------|-------------|
| Pin hero image | `{pin_id}-hero.{ext}` | `W12-01-hero.png` | `data/generated/pins/` |
| Blog hero image (copy) | `{slug}-hero.{ext}` | `easy-chicken-tacos-hero.png` | `data/generated/pins/` |
| Rendered pin | `{pin_id}.png` | `W12-01.png` | `data/generated/pins/` |
| AI comparison hero | `{pin_id}-ai-hero.png` | `W12-01-ai-hero.png` | `data/generated/pins/` |
| Regen hero | `{pin_id}-hero-regen.png` | `W12-01-hero-regen.png` | `data/generated/pins/` |

### Image IDs (for dedup tracking)
| Source | Format | Example |
|--------|--------|---------|
| Unsplash | `unsplash:{photo_id}` | `unsplash:abc123xyz` |
| Pexels | `pexels:{photo_id}` | `pexels:12345678` |
| AI generated | `ai_{md5_hash[:12]}` | `ai_a1b2c3d4e5f6` |
| Template-only | `""` (empty string) | |

- **Stored in:** `content-log.jsonl` and `pin-generation-results.json` → `image_id` field
- **Dedup window:** 90 days (via `load_used_image_ids()` in `generate_pin_content.py`)

---

## 3. File Path Conventions

### Generated Content (data/generated/)
```
data/generated/
├── blog/
│   ├── easy-weeknight-chicken-tacos.mdx     # Blog post (slug-named)
│   ├── 7-slow-cooker-dinners.mdx
│   └── ...
└── pins/
    ├── W12-01-hero.png          # Pin hero image (pin_id-named)
    ├── W12-01.png               # Rendered pin (1000x1500)
    ├── W12-01-ai-hero.png       # AI comparison hero (being removed)
    ├── easy-weeknight-chicken-tacos-hero.png  # Blog hero image COPY (slug-named)
    └── ...
```

### Dual Hero Image Save (generate_pin_content.py:182-190)

When a pin's hero image is sourced, it's saved with the pin_id name (e.g., `W12-01-hero.png`). Then a COPY is made with the blog slug name (e.g., `easy-chicken-tacos-hero.png`). This is because:

- **Pin assembly** uses the pin_id-named file (`pin_assembler.assemble_pin(hero_image_path=...)`)
- **Blog deployment** looks for the slug-named file (`blog_deployer.py` → commits `{slug}-hero.{ext}` to goslated.com)
- **GCS upload** uses the pin_id-named file for Content Queue previews

```python
# generate_pin_content.py lines 182-190
blog_slug = _resolve_blog_slug(pin_spec, blog_posts)
if blog_slug and image_path and Path(image_path).exists():
    slug_hero = PIN_OUTPUT_DIR / f"{blog_slug}-hero{Path(image_path).suffix}"
    if not slug_hero.exists():
        shutil.copy2(image_path, slug_hero)
```

### Data Files (data/)
```
data/
├── weekly-plan-W12-2026-02-24.json    # Current week's plan
├── pin-generation-results.json         # Pin generation output (updated by publish + regen)
├── blog-generation-results.json        # Blog generation output
├── pin-schedule.json                   # Posting schedule (created at promote step)
├── content-log.jsonl                   # Append-only posting log (one JSON per line)
└── token-store.json                    # Pinterest OAuth tokens (NEVER read)
```

### Deployed to goslated.com
```
goslated.com repo:
├── content/blog/{slug}.mdx                    # Blog posts
└── public/assets/blog/{slug}-hero.{ext}       # Hero images
```

---

## 4. Image Flow (Detailed)

### Current Architecture (Pre-Simplification)

```
generate_pin_content()
  │
  ├─ pin_spec.get("image_source_tier", "stock")     ← from weekly plan
  │
  ├─ _source_pin_image()                             ← WRAPPER
  │  │
  │  ├─ source_image()                               ← ROUTER by tier
  │  │  ├─ "stock" → _source_stock_image()
  │  │  │   ├─ claude.generate_image_prompt(image_source="stock")
  │  │  │   ├─ stock_api.search() → Unsplash/Pexels
  │  │  │   ├─ stock_api.filter_previously_used()
  │  │  │   ├─ claude.rank_stock_candidates() [Haiku vision]
  │  │  │   ├─ If score < 6.5: retry with broader query
  │  │  │   ├─ If retry < 5.0: fall back to _source_ai_image()
  │  │  │   └─ stock_api.download_image()
  │  │  │
  │  │  ├─ "ai" → _source_ai_image()
  │  │  │   ├─ claude.generate_image_prompt(image_source="ai")
  │  │  │   ├─ image_gen_api.generate() [gpt-image-1.5]
  │  │  │   ├─ claude.validate_ai_image() [Sonnet vision]
  │  │  │   ├─ If score < 6.5: regen with feedback, re-validate
  │  │  │   └─ If still < 6.5: accept with low_confidence=True
  │  │  │
  │  │  └─ "template" → (None, "template", "", {})
  │  │
  │  └─ [If winner is NOT AI] Generate AI comparison image
  │     └─ _source_ai_image(filename_prefix="{pin_id}-ai")
  │        └─ Store in quality_meta: _ai_hero_image_path, _ai_image_id, _ai_image_score
  │
  ├─ Dual save: {pin_id}-hero.{ext} + {slug}-hero.{ext}
  │
  └─ pin_assembler.assemble_pin(hero_image_path=...)
     └─ Puppeteer renders HTML → PNG (1000x1500)
```

### After Simplification (Proposed)

```
generate_pin_content()
  │
  ├─ Check pin_template
  │  ├─ "infographic-pin" → template-only (no image needed)
  │  └─ all others → _source_ai_image()
  │
  ├─ _source_ai_image()  (simplified)
  │  ├─ claude.generate_image_prompt(pin_spec)  [GPT-5 Mini, fallback Sonnet]
  │  ├─ Append _regen_feedback if present
  │  ├─ image_gen_api.generate(prompt, 1000, 1500)  [gpt-image-1.5]
  │  ├─ Hash prompt → image_id = "ai_{hash}"
  │  └─ Return (path, "ai_generated", image_id, quality_meta)
  │
  ├─ Dual save: {pin_id}-hero.png + {slug}-hero.png
  │
  └─ pin_assembler.assemble_pin(hero_image_path=...)
     └─ Puppeteer renders HTML → PNG (1000x1500)
```

### Key Functions and Return Types

**`_source_ai_image()` returns:**
```python
(
    image_path: Path,           # e.g., data/generated/pins/W12-01-hero.png
    image_source: str,          # "ai_generated"
    image_id: str,              # "ai_a1b2c3d4e5f6"
    quality_meta: dict          # {"image_retries": 0, ...}
)
```

**`source_image()` returns:** Same tuple, routes based on tier.

**`_source_pin_image()` returns:** Same tuple, but quality_meta may also contain `_ai_hero_image_path`, `_ai_image_id`, `_ai_image_score` (comparison image data).

---

## 5. Pin Assembly & Template Variables

### Template Types and Required Variables

| Template | Image Variable | Other Required | No Image |
|----------|---------------|----------------|----------|
| recipe-pin | `hero_image_url` | `headline`, `subtitle` | |
| tip-pin | `background_image_url` | `headline`, `bullet_1`, `bullet_2`, `bullet_3` | |
| listicle-pin | `background_image_url` | `number`, `headline`, `list_items` | |
| problem-solution-pin | `background_image_url` | `problem_text`, `solution_text` | |
| infographic-pin | — | `title`, `steps`, `footer_text` | YES |

### How Image Variables Are Set

**In `assemble_pin()` (pin_assembler.py:477-508):**
```python
context = {"headline": headline, "subtitle": subtitle}
if hero_image_path:
    context["hero_image_url"] = str(hero_image_path)    # For recipe-pin
if extra_context:
    context.update(extra_context)                         # Includes background_image_url
```

**In `build_template_context()` (pin_assembler.py:576-582):**
```python
# Non-recipe templates need background_image_url
if template_type != "recipe-pin" and image_path:
    context["background_image_url"] = str(image_path)
```

**At render time (`_prepare_html()`, pin_assembler.py:296-304):**
Both `hero_image_url` and `background_image_url` are converted from local file paths to data URIs:
```python
image_vars = ["hero_image_url", "background_image_url"]
for var in image_vars:
    if "{{" + var + "}}" in html:
        value = context.get(var, "")
        if value and not value.startswith(("data:", "http://", "https://")):
            value = _image_to_data_uri(value)  # Local path → base64 data URI
```

### Call Site in generate_pin_content.py

```python
# Lines ~165-180
template_context = build_template_context(
    pin_copy=pin_copy,
    template_type=template_type,
    image_path=image_path,      # hero image local path
)
rendered_pin_path = assembler.assemble_pin(
    template_type=template_type,
    hero_image_path=str(image_path) if image_path else None,
    headline=pin_copy.get("headline", pin_copy.get("title", "")),
    subtitle=pin_copy.get("subtitle", ""),
    variant=pin_spec.get("treatment_number", 1),
    output_path=PIN_OUTPUT_DIR / f"{pin_id}.png",
    extra_context=template_context,
)
```

---

## 6. Data File Schemas

### weekly-plan-W{N}-{date}.json
```json
{
  "week_number": "W12",
  "date_range": "Feb 24 - Mar 2, 2026",
  "blog_posts": [{
    "post_id": "W12-P01",
    "topic": "Easy weeknight chicken tacos",
    "content_type": "recipe",
    "pillar": 1,
    "primary_keyword": "easy chicken tacos",
    "secondary_keywords": ["weeknight tacos", "quick dinner"],
    "image_source_tier": "stock"
  }],
  "pins": [{
    "pin_id": "W12-01",
    "source_post_id": "W12-P01",
    "pin_topic": "Chicken taco hero shot",
    "pin_template": "recipe-pin",
    "target_board": "Quick Family Dinners",
    "scheduled_date": "2026-02-25",
    "scheduled_slot": "morning",
    "image_source_tier": "stock",
    "funnel_layer": "discovery",
    "treatment_number": 1
  }]
}
```

### blog-generation-results.json
```json
{
  "W12-P01": {
    "slug": "easy-weeknight-chicken-tacos",
    "title": "Easy Weeknight Chicken Tacos",
    "file_path": "data/generated/blog/easy-weeknight-chicken-tacos.mdx",
    "pillar": 1,
    "content_type": "recipe",
    "mdx_content": "---\ntitle: Easy Weeknight...\n---\n\n...",
    "status": "success"
  }
}
```

### pin-generation-results.json
```json
{
  "generated": [{
    "pin_id": "W12-01",
    "title": "25-Minute Chicken Tacos",
    "description": "Quick weeknight chicken tacos...",
    "alt_text": "Overhead view of chicken tacos...",
    "text_overlay": {"headline": "Easy Chicken Tacos", "sub_text": "Ready in 25 min"},
    "image_path": "data/generated/pins/W12-01.png",
    "hero_image_path": "data/generated/pins/W12-01-hero.png",
    "image_source": "ai_generated",
    "image_id": "ai_a1b2c3d4e5f6",
    "board_name": "Quick Family Dinners",
    "board_id": "123456789",
    "link": "https://goslated.com/blog/easy-weeknight-chicken-tacos",
    "blog_slug": "easy-weeknight-chicken-tacos",
    "template": "recipe-pin",
    "content_type": "recipe",
    "primary_keyword": "easy chicken tacos",
    "secondary_keywords": ["weeknight tacos"],
    "pillar": 1,
    "funnel_layer": "discovery",
    "source_post_id": "W12-P01",
    "scheduled_date": "2026-02-25",
    "scheduled_slot": "morning",
    "treatment_number": 1,
    "image_quality_score": 7.5,
    "image_retries": 0,
    "image_low_confidence": false,
    "image_source_original": "ai_generated",
    "image_quality_issues": [],
    "_ai_hero_image_path": "data/generated/pins/W12-01-ai-hero.png",
    "_ai_image_id": "ai_f6e5d4c3b2a1",
    "_ai_image_score": 7.1,
    "_drive_image_url": "https://storage.googleapis.com/bucket/W12-01.png",
    "_drive_download_url": "https://storage.googleapis.com/bucket/W12-01.png",
    "_ai_image_url": "https://storage.googleapis.com/bucket/ai-heroes/W12-01-ai-hero.png"
  }],
  "failures": [{"pin_id": "W12-02", "error": "Image generation timeout"}]
}
```

**Fields prefixed with `_` are internal/transient** — added by publish_content_queue.py or regen_content.py, not by generate_pin_content.py itself:
- `_drive_image_url` — GCS public URL for the rendered pin
- `_drive_download_url` — GCS direct download URL (used as `image_url` in pin-schedule.json)
- `_ai_image_url` — GCS URL for AI comparison hero image
- `_ai_hero_image_path` — Local path to AI comparison hero (set during generation)
- `_ai_image_id`, `_ai_image_score` — AI comparison metadata

### pin-schedule.json
```json
{
  "2026-02-25": {
    "morning": [{
      "pin_id": "W12-01",
      "title": "25-Minute Chicken Tacos",
      "description": "Quick weeknight chicken tacos...",
      "alt_text": "Overhead view of chicken tacos...",
      "board_id": "123456789",
      "board_name": "Quick Family Dinners",
      "link": "https://goslated.com/blog/easy-weeknight-chicken-tacos",
      "image_path": "data/generated/pins/W12-01.png",
      "image_url": "https://storage.googleapis.com/bucket/W12-01.png",
      "scheduled_date": "2026-02-25",
      "scheduled_slot": "morning",
      "pillar": 1,
      "pin_type": "primary",
      "primary_keyword": "easy chicken tacos",
      "secondary_keywords": ["weeknight tacos"],
      "blog_slug": "easy-weeknight-chicken-tacos",
      "content_type": "recipe",
      "funnel_layer": "discovery",
      "template": "recipe-pin",
      "treatment_number": 1,
      "source_post_id": "W12-P01",
      "image_source": "ai_generated",
      "image_id": "ai_a1b2c3d4e5f6",
      "status": "scheduled"
    }],
    "afternoon": [],
    "evening": []
  }
}
```

**CRITICAL:** `image_url` comes from `_drive_download_url` in pin-generation-results.json (set at `blog_deployer.py:745`). This is the GCS public URL that Pinterest will fetch the image from.

**CRITICAL:** `link` at this stage is the BARE blog URL (no UTM params). UTM params are added by `post_pins.py` at posting time via `construct_utm_link()`.

### content-log.jsonl (one JSON per line)
```json
{
  "pin_id": "W12-01",
  "pinterest_pin_id": "pinterest-123456789",
  "posted_date": "2026-02-25",
  "posted_slot": "morning",
  "board": "Quick Family Dinners",
  "board_id": "123456789",
  "title": "25-Minute Chicken Tacos",
  "blog_slug": "easy-weeknight-chicken-tacos",
  "blog_url": "https://goslated.com/blog/easy-weeknight-chicken-tacos",
  "link": "https://goslated.com/blog/easy-weeknight-chicken-tacos?utm_source=pinterest&utm_medium=organic&utm_campaign=quick-family-dinners&utm_content=W12-01",
  "pillar": 1,
  "content_type": "recipe",
  "funnel_layer": "discovery",
  "template": "recipe-pin",
  "primary_keyword": "easy chicken tacos",
  "secondary_keywords": ["weeknight tacos"],
  "image_source": "ai_generated",
  "image_id": "ai_a1b2c3d4e5f6",
  "pin_type": "primary",
  "treatment_number": 1,
  "source_post_id": "W12-P01",
  "impressions": 0,
  "saves": 0,
  "pin_clicks": 0,
  "outbound_clicks": 0,
  "save_rate": 0.0,
  "click_through_rate": 0.0,
  "last_analytics_pull": null
}
```

**Two writers:**
1. `blog_deployer.py` → `_append_to_content_log()` — Creates initial entries with `pin_id: null` (placeholder before posting)
2. `post_pins.py` — Creates/updates entries with actual `pinterest_pin_id` from Pinterest API response

---

## 7. Google Sheets Column Layout

### Weekly Review Tab
| Row | Col A | Col B | Col C | Col D |
|-----|-------|-------|-------|-------|
| 1 | "Pinterest Weekly Review" | | | date |
| 3 | "STATUS" | `pending_review` / `approved` | | |
| 4 | "PRODUCTION" | `pending_review` / `approved` | preview_url | |
| 5 | "PLAN REGEN" | `idle` / `regen` | | (NEW — Change 4) |
| 6+ | Plan summary: blog post rows, pin rows, metrics | | | |

### Content Queue Tab (Current — Before Simplification)
| Col Index | Letter | Name | Notes |
|-----------|--------|------|-------|
| 0 | A | ID | `W12-01` (pins) or `W12-P01` (blogs) |
| 1 | B | Type | `"pin"` or `"blog"` |
| 2 | C | Title | |
| 3 | D | Description | |
| 4 | E | Board | Pins only |
| 5 | F | Blog URL | Slug for blogs, full link for pins |
| 6 | G | Schedule | `"YYYY-MM-DD/slot"` |
| 7 | H | Pillar | 1-5 |
| 8 | I | Thumbnail | `=IMAGE("gcs_url")` formula |
| 9 | J | Status | `pending_review` / `approved` / `rejected` / `use_ai_image` / `regen_*` |
| 10 | K | Notes | Quality gate notes |
| 11 | L | Feedback | Reviewer feedback for regen |
| 12 | M | AI Image | `=IMAGE("ai_gcs_url")` — AI comparison |
| 13 | N | Regen Label | `"Regen >"` (in row 1 only) |
| 14 | O | Regen Trigger | `"idle"` / `"run"` (in row 1 only) |

**Column constants in sheets_api.py:**
```python
CQ_COL_ID         = 0   # A
CQ_COL_TYPE       = 1   # B
CQ_COL_TITLE      = 2   # C
CQ_COL_DESCRIPTION= 3   # D
CQ_COL_BOARD      = 4   # E
CQ_COL_BLOG_URL   = 5   # F
CQ_COL_SCHEDULE   = 6   # G
CQ_COL_PILLAR     = 7   # H
CQ_COL_THUMBNAIL  = 8   # I
CQ_COL_STATUS     = 9   # J
CQ_COL_NOTES      = 10  # K
CQ_COL_FEEDBACK   = 11  # L
CQ_COL_AI_IMAGE   = 12  # M
```

### Content Queue Tab (After Simplification)
| Col Index | Letter | Name |
|-----------|--------|------|
| 0-11 | A-L | Same (ID through Feedback) |
| 12 | M | Regen Label `"Regen >"` |
| 13 | N | Regen Trigger `"idle"` / `"run"` |

Column M ("AI Image") removed. Regen trigger shifts from N1:O1 to M1:N1.

### Column Shift Impact (Simplification)

| Location | Current | After |
|----------|---------|-------|
| `sheets_api.py` `CQ_COL_AI_IMAGE` | `= 12` (Column M) | DELETED |
| `sheets_api.py` `read_content_approvals()` range | `A:M` | `A:L` |
| `sheets_api.py` `read_regen_requests()` range | `A:M` | `A:L` |
| `sheets_api.py` `reset_regen_trigger()` cell | `O1` | `N1` |
| `publish_content_queue.py` regen trigger range | `N1:O1` | `M1:N1` |
| `trigger.gs` `onSheetEdit()` column check | `getColumn() === 15` | `getColumn() === 14` |
| `trigger.gs` `runRegen()` cell | `"O1"` | `"N1"` |

### Terminal Statuses (for deploy gate)
**Current:** `["approved", "rejected", "use_ai_image"]`
**After simplification:** `["approved", "rejected"]`

---

## 8. GCS Upload Flow

### Sequence (publish_content_queue.py)

```
1. gcs.upload_pin_images(generated_pins, PIN_OUTPUT_DIR)
   → Uploads rendered pins: {pin_id}.png
   → Returns: {pin_id: gcs_public_url}
   → Stored as pin["_drive_image_url"] and pin["_drive_download_url"]

2. gcs.upload_blog_hero_images(blog_results, PIN_OUTPUT_DIR)
   → Uploads blog heroes: {slug}-hero.{ext}
   → Returns: {post_id: gcs_public_url}

3. gcs.upload_ai_hero_images(generated_pins, PIN_OUTPUT_DIR)
   → Uploads AI comparison heroes: {pin_id}-ai-hero.png
   → Returns: {pin_id: gcs_public_url}
   → Stored as pin["_ai_image_url"]

4. Save updated pin-generation-results.json (now with GCS URLs)

5. sheets.write_content_queue(
     blogs, pins,
     pin_image_urls,         # For Column I thumbnails
     blog_image_urls,        # For blog row thumbnails
     ai_image_urls,          # For Column M AI comparison
     blog_ai_image_urls,     # For blog row AI comparison
   )

6. Write regen trigger cells: N1:O1 = ["Regen >", "idle"]
```

### GCS URL Format
- Rendered pins: `https://storage.googleapis.com/{bucket}/W12-01.png`
- AI heroes: `https://storage.googleapis.com/{bucket}/ai-heroes/W12-01-ai-hero.png`
- Blog heroes: `https://storage.googleapis.com/{bucket}/blog-heroes/easy-chicken-tacos-hero.png`

---

## 9. Blog Deployment & Pin Schedule

### deploy_to_preview() Flow (blog_deployer.py)
```
1. Filter approved blogs and pins (status in ["approved", "use_ai_image"])
2. For each approved blog:
   a. Read {slug}.mdx from data/generated/blog/
   b. Find {slug}-hero.{ext} from data/generated/pins/
   c. Commit to goslated.com develop branch:
      - content/blog/{slug}.mdx
      - public/assets/blog/{slug}-hero.{ext}
3. Vercel auto-deploys develop branch → preview URL
4. verify_urls() — HTTP GET each blog URL on preview domain
5. Write deploy status to Weekly Review sheet
```

### promote_to_production() Flow (blog_deployer.py)
```
1. Merge develop → main on goslated.com repo
2. verify_urls() on production domain
3. [If use_ai_image pins exist] _process_ai_image_swaps()
   → Downloads AI hero, re-renders pin, re-uploads to GCS
   → Updates _drive_download_url in pin-generation-results.json
4. _create_pin_schedule() → pin-schedule.json
5. _append_to_content_log() → content-log.jsonl (initial entries)
6. Git commit + push pin-schedule.json and content-log.jsonl
```

### _create_pin_schedule() Field Mapping (blog_deployer.py:736-761)

| pin-schedule.json field | Source (from pin-generation-results.json) |
|------------------------|------------------------------------------|
| `pin_id` | `pin_id` |
| `title` | `title` |
| `description` | `description` |
| `alt_text` | `alt_text` |
| `board_id` | `board_id` |
| `board_name` | `board_name` |
| `link` | `link` (bare blog URL, no UTM) |
| `image_path` | `image_path` (local rendered pin path) |
| **`image_url`** | **`_drive_download_url`** (GCS public URL) |
| `scheduled_date` | `scheduled_date` |
| `scheduled_slot` | `scheduled_slot` |
| `image_source` | `image_source` |
| `image_id` | `image_id` |
| `blog_slug` | `blog_slug` |
| `content_type` | `content_type` |
| `template` | `template` |
| `source_post_id` | `source_post_id` |
| `treatment_number` | `treatment_number` |
| `funnel_layer` | `funnel_layer` |

---

## 10. Daily Posting Flow

### post_pins.py Execution

```
1. load_scheduled_pins(today_str, time_slot)
   → Reads pin-schedule.json
   → Filters by scheduled_date + scheduled_slot

2. For each scheduled pin:
   a. Resolve board_id from board_name
   b. construct_utm_link(blog_url, board_name, pin_id)
      → "{blog_url}?utm_source=pinterest&utm_medium=organic
          &utm_campaign={board_slug}&utm_content={pin_id}"
   c. pinterest.create_pin(
        board_id, title, description, link,
        image_url=image_url,  # GCS URL from pin-schedule.json
        alt_text=alt_text
      )
   d. Get back pinterest_pin_id from API response
   e. Append to content-log.jsonl with full metadata + pinterest_pin_id
```

### UTM Parameter Format
```
?utm_source=pinterest
&utm_medium=organic
&utm_campaign={board_name_slugified}    # e.g., "quick-family-dinners"
&utm_content={pin_id}                   # e.g., "W12-01"
```

The board name is slugified: `re.sub(r"[^a-z0-9]+", board_name.lower()).strip("-")`

---

## 11. Content Log & Analytics

### Two Writers to content-log.jsonl

1. **blog_deployer.py** (`_append_to_content_log()`) — Creates initial entries at promote time
   - `pin_id` is set (schedule ID like "W12-01")
   - `pinterest_pin_id` is null (not yet posted)
   - All metadata from pin-generation-results.json

2. **post_pins.py** — Creates entries at posting time
   - `pinterest_pin_id` is set (from Pinterest API)
   - `link` includes UTM parameters
   - Dedup: checks existing `schedule_id` / `pin_id` to avoid duplicates

### Analytics Update Flow
```
pull_analytics.py:
  → pinterest.get_pin_analytics(pinterest_pin_id)
  → Updates: impressions, saves, pin_clicks, outbound_clicks, save_rate, click_through_rate
  → Sets: last_analytics_pull timestamp
```

### Aggregation Dimensions (used by weekly_analysis.py + monthly_review.py)
- `image_source` — Groups by stock/ai_generated/template
- `content_type` — Groups by recipe/guide/listicle/weekly-plan
- `template` — Groups by pin template type
- `funnel_layer` — Groups by discovery/consideration/conversion
- `pillar` — Groups by content pillar 1-5
- `board` — Groups by Pinterest board

---

## 12. Apps Script Triggers

### trigger.gs Cell Watchers

| Tab | Cell/Column | Value | Dispatches | Workflow |
|-----|-------------|-------|------------|----------|
| Weekly Review | B3 | `"approved"` | `generate-content` | generate-content.yml |
| Weekly Review | B4 | `"approved"` | `promote-and-schedule` | promote-and-schedule.yml |
| Weekly Review | B5 | `"regen"` | `regen-plan` | regen-plan.yml (NEW) |
| Content Queue | Column J (all rows) | All terminal | `deploy-to-preview` | deploy-and-schedule.yml |
| Content Queue | O1 (→ N1 after simplification) | `"run"` | `regen-content` | regen-content.yml |

### allContentReviewed() Logic
- Reads all non-header rows in Content Queue
- Checks Column J (Status) for each row
- If ALL statuses are in the terminal set, triggers deploy-to-preview
- **Current terminal set:** `["approved", "rejected", "use_ai_image"]`
- **After simplification:** `["approved", "rejected"]`

### Column Number References in Apps Script
- **Current:** `range.getColumn() === 15` (column O, 1-based) for regen trigger
- **After simplification:** Must change to `range.getColumn() === 14` (column N, 1-based)
- `runRegen()` button function: Writes to `"O1"` → must change to `"N1"`

---

## 13. LLM Model Calls

### Model Constants (claude_api.py)
```python
MODEL_ROUTINE = "claude-sonnet-4-20250514"    # $3/$15 per MTk
MODEL_DEEP    = "claude-opus-4-20250514"      # $15/$75 per MTk
MODEL_HAIKU   = "claude-haiku-4-5-20251001"   # $0.80/$4 per MTk
```

### Call Inventory

| # | Function | Current Model | After Simplification | Prompt File | Cost/call |
|---|----------|--------------|---------------------|-------------|-----------|
| 1 | `generate_weekly_plan()` | Sonnet | Sonnet (unchanged) | `weekly_plan.md` | ~$0.10 |
| 2 | `generate_pin_copy()` | Sonnet | **GPT-5 Mini** (fallback Sonnet) | `pin_copy.md` | ~$0.001 |
| 3 | `generate_blog_post()` | Sonnet | Sonnet (unchanged) | `blog_post_*.md` | ~$0.15 |
| 4 | `generate_image_prompt()` | Sonnet | **GPT-5 Mini** (fallback Sonnet) | `image_prompt.md` | ~$0.001 |
| 5 | `rank_stock_candidates()` | Haiku + vision | **REMOVED** | `image_rank.md` (DELETE) | — |
| 6 | `validate_ai_image()` | Sonnet + vision | **REMOVED** | `image_validate.md` (DELETE) | — |
| 7 | `generate_replacement_posts()` | Sonnet | Sonnet (unchanged) | `weekly_plan_replace.md` | ~$0.07 |
| 8 | `analyze_weekly_performance()` | Sonnet | Sonnet (unchanged) | `weekly_analysis.md` | ~$0.08 |
| 9 | `run_monthly_review()` | Opus | Opus (unchanged) | `monthly_review.md` | ~$0.12 |

### Image Generation (non-LLM)
| Provider | Model | Use | Cost |
|----------|-------|-----|------|
| OpenAI | `gpt-image-1.5` | Generate hero images from text prompt | ~$0.05/image |

---

## 14. Workflow Execution Order

### Weekly Cycle (typical)

```
Monday 6:00 AM ET
  └─ weekly-review.yml
     ├─ pull_analytics.py          (update content-log.jsonl metrics)
     ├─ weekly_analysis.py         (Claude analyzes last week)
     └─ generate_weekly_plan.py    (Claude generates next week's plan)
        └─ Writes Weekly Review sheet → STATUS = "pending_review"

Monday ~morning (human)
  └─ Set B3 = "approved" in Weekly Review
     └─ Apps Script → generate-content.yml
        ├─ generate_blog_posts.py  (8-10 blog MDX files)
        ├─ generate_pin_content.py (28 pins: copy + images + render)
        └─ publish_content_queue.py (GCS upload + Content Queue sheet)

Monday-Tuesday (human)
  └─ Review Content Queue, set statuses, optionally regen
     └─ If regen needed: set N1 = "run" → regen-content.yml
     └─ When all rows terminal → Apps Script → deploy-and-schedule.yml
        └─ blog_deployer.deploy_to_preview()

Tuesday (human)
  └─ Check preview URLs, set B4 = "approved"
     └─ Apps Script → promote-and-schedule.yml
        └─ blog_deployer.promote_to_production()
           ├─ Merge develop → main
           ├─ Create pin-schedule.json
           └─ Initialize content-log.jsonl entries

Tuesday-Monday (automated)
  └─ daily-post-morning.yml     (10:00 AM ET)
  └─ daily-post-afternoon.yml   (3:00 PM ET)
  └─ daily-post-evening.yml     (8:00 PM ET)
     └─ post_pins.py → Pinterest API
```

### GitHub Actions Workflow Files
| Workflow | Trigger | Script |
|----------|---------|--------|
| `weekly-review.yml` | Cron: Monday 6am ET | analytics + plan |
| `generate-content.yml` | Dispatch: `generate-content` | blogs + pins + publish |
| `regen-content.yml` | Dispatch: `regen-content` | selective regen |
| `deploy-and-schedule.yml` | Dispatch: `deploy-to-preview` | deploy preview |
| `promote-and-schedule.yml` | Dispatch: `promote-and-schedule` | promote + schedule |
| `daily-post-morning.yml` | Cron: 10am ET | post pins |
| `daily-post-afternoon.yml` | Cron: 3pm ET | post pins |
| `daily-post-evening.yml` | Cron: 8pm ET | post pins |
| `monthly-review.yml` | Cron: 1st Monday | monthly analysis |
| `regen-plan.yml` | Dispatch: `regen-plan` | plan regen (NEW) |

---

## 15. Cross-File Dependency Map

### Who Writes / Who Reads Each Data File

| Data File | Written By | Read By |
|-----------|-----------|---------|
| `weekly-plan-W{N}-{date}.json` | `generate_weekly_plan.py` | `generate_blog_posts.py`, `generate_pin_content.py` |
| `blog-generation-results.json` | `blog_generator.py` | `generate_pin_content.py`, `publish_content_queue.py`, `blog_deployer.py` |
| `pin-generation-results.json` | `generate_pin_content.py` | `publish_content_queue.py` (adds GCS URLs), `regen_content.py` (updates), `blog_deployer.py` (reads + updates via AI swap) |
| `pin-schedule.json` | `blog_deployer.py` | `post_pins.py`, `regen_content.py` (updates if exists) |
| `content-log.jsonl` | `blog_deployer.py` (initial), `post_pins.py` (posted) | `pull_analytics.py` (update), `weekly_analysis.py`, `monthly_review.py`, `generate_weekly_plan.py` (memory) |

### Key Cross-File Field Flows

**blog_slug flow:**
```
Claude generates slug in blog frontmatter
  → blog_generator.py extracts from frontmatter → blog-generation-results.json["slug"]
  → generate_pin_content.py reads blog results → pin_data["blog_slug"]
  → dual-save hero image: {slug}-hero.{ext}
  → blog_deployer.py reads blog results + pin results → commits {slug}.mdx + {slug}-hero.{ext}
  → pin-schedule.json["blog_slug"]
  → content-log.jsonl["blog_slug"]
```

**image_url flow:**
```
image_gen_api.generate() → saves {pin_id}-hero.png locally
  → pin_assembler renders → saves {pin_id}.png locally
  → gcs.upload_pin_images() → GCS URL
  → pin["_drive_download_url"] = GCS URL (in pin-generation-results.json)
  → blog_deployer._create_pin_schedule() → pin-schedule.json["image_url"] = _drive_download_url
  → post_pins.py → pinterest.create_pin(image_url=...)
```

**link flow:**
```
generate_pin_content.py → pin_data["link"] = "https://goslated.com/blog/{slug}" (bare URL)
  → pin-generation-results.json["link"]
  → pin-schedule.json["link"] (still bare URL)
  → post_pins.py → construct_utm_link() adds UTM params
  → pinterest.create_pin(link=utm_link)
  → content-log.jsonl["link"] = utm_link
```

### Import Dependencies (key cross-file imports)

```
regen_content.py imports from generate_pin_content.py:
  - generate_copy_batch
  - load_used_image_ids
  - source_image          # Public router
  - _source_ai_image      # Private AI function (direct import)
  - _load_brand_voice
  - _load_keyword_targets
  - build_template_context

blog_deployer.py imports from:
  - pin_assembler (for re-rendering during AI swaps)
  - gcs_api (for re-uploading during AI swaps)
  - github_api (for committing to goslated.com)
  - sheets_api (for status updates)

publish_content_queue.py imports from:
  - gcs_api (primary upload)
  - drive_api (fallback upload)
  - sheets_api (Content Queue write)
```

---

## 16. Environment Variables

### Required for all workflows
- `ANTHROPIC_API_KEY` — Claude API
- `OPENAI_API_KEY` — Image generation (gpt-image-1.5) + image prompts (gpt-5-mini)
- `GOOGLE_SHEETS_CREDENTIALS_JSON` — Sheets + GCS authentication
- `GOOGLE_SHEET_ID` — Target spreadsheet
- `GCS_BUCKET_NAME` — Google Cloud Storage bucket
- `PINTEREST_ACCESS_TOKEN` — Pinterest API
- `PINTEREST_ENVIRONMENT` — "production" or "sandbox"
- `SLACK_WEBHOOK_URL` — Notifications
- `GITHUB_TOKEN` — Git operations on goslated.com repo
- `GOSLATED_REPO` — Target repo for blog deployment

### Optional
- `IMAGE_GEN_PROVIDER` — "openai" (default) or "replicate"
- `REPLICATE_API_TOKEN` — Only if using Flux Pro

---

## Appendix: Regen Content Flow

### How regen_content.py Works

```
1. sheets.read_regen_requests()
   → Reads Content Queue, filters for status.startswith("regen")
   → Returns list of {pin_id, status, feedback}

2. For each flagged item:
   a. Load current pin data from pin-generation-results.json
   b. Based on status:
      - regen_image → Source new image (respects feedback), re-render pin
      - regen_copy  → Generate new copy (respects feedback), re-render pin
      - regen       → Both image and copy
   c. Upload new rendered pin to GCS
   d. [Current] Generate AI comparison image, upload to GCS
   e. Update pin-generation-results.json in-place
   f. Update Sheet row (thumbnail, title, description, notes, status → "pending_review")

3. If pin-schedule.json exists, update image_url for affected pins

4. Git commit + push updated files
5. Slack notification
```

### regen_content.py Key Imports from generate_pin_content.py
- `source_image()` — Used for image regen (routes by tier)
- `_source_ai_image()` — Used for AI comparison generation
- `generate_copy_batch()` — Used for copy regen
- `build_template_context()` — Used for re-rendering pins
- `load_used_image_ids()` — Used for dedup during regen
