name: TikTok Generate Content

# Triggered by Apps Script when Weekly Review B3 = "approved" (via repository_dispatch)
on:
  repository_dispatch:
    types: [tiktok-generate-content]
  workflow_dispatch:  # Manual fallback trigger

concurrency:
  group: slated-content-engine-tiktok
  cancel-in-progress: false

env:
  ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  GOOGLE_SHEETS_CREDENTIALS_JSON: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS_JSON }}
  TIKTOK_SPREADSHEET_ID: ${{ secrets.TIKTOK_SPREADSHEET_ID }}
  GCS_BUCKET_NAME: ${{ secrets.GCS_BUCKET_NAME }}
  SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

jobs:
  tiktok-generate-content:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull latest (plan JSON from previous runner)
        run: git pull --rebase origin main

      - name: Setup pipeline
        uses: ./.github/actions/setup-pipeline

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Puppeteer
        run: npm install puppeteer

      - name: Render approved carousel specs
        run: python -m src.tiktok.generate_weekly_plan --generate-content

      - name: Commit updated plan (render data)
        uses: ./.github/actions/commit-data
        with:
          message: "chore: generated TikTok carousel content [skip ci]"

      - name: Notify failure
        if: failure()
        uses: ./.github/actions/notify-failure
        with:
          workflow_name: "TikTok Generate Content"
          run_url: "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
